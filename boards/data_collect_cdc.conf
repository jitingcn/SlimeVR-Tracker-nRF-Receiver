# Enable raw sensor data collection via CDC serial port
CONFIG_DATA_COLLECT=y
CONFIG_DATA_COLLECT_HID=n
# Keep CDC ACM buffers modest: this build creates two CDC ACM interfaces,
# so the global ringbuf size is multiplied across multiple RX/TX buffers.
CONFIG_USB_CDC_ACM_RINGBUF_SIZE=4096
